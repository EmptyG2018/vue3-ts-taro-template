import pkage from '../package.json';

export default {
  // 版本
  version: pkage.version,

  // 应用标题
  title: '柏溪官方商城',

  // host请求地址，可在package.json配置
  host: REQUEST_URL,

  // 请求时长（ms）
  timeout: 2000,
};
